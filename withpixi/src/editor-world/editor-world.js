/**
 * Created by chuhaoyuan on 2017/8/30.
 */
import {BaseWorld, Inherited, Eventuality} from './../utility/imports'
const EditorWorld = function () {
  var that = Inherited(BaseWorld());

  return that;
};
export default EditorWorld;